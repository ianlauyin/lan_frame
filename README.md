# lan_frame

A rust framework for myself using axum, tokio and mysql

### DevTool

Use `cargo install systemfd cargo-watch` to install

Run `systemfd --no-pid -s http::3000 -- cargo watch -x run` to enable recomplie when code is changed
